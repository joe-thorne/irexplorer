services:
  toolchain:
    build:
      context: .
      dockerfile: Dockerfile.toolchain
    image: irexplorer-toolchain:llvm-22.1.8-ubuntu24.04
    platform: linux/amd64
    working_dir: /workspace
    volumes:
      - .:/workspace
